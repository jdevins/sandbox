node_modules/
npm-debug.log*
.env
.DS_Store
*.local
.claude/

# markitdown app: Python virtualenv (rebuilt by `npm run markitdown:setup`)
apps/markitdown/service/.venv/
apps/markitdown/service/build/
apps/markitdown/dist/
apps/markitdown/data/runs/
__pycache__/

# repacks (machine-local raw transcript output; shared between machines via
# explicit export/import, not git)
apps/claude-engine/data/repacks
apps/claude-engine/data/repack-imports

# LLM observability event log (src/llmObserver.js) — machine-local operational
# history, one JSONL per day, not repo state
data/runs/

# Sandbox runtime state (storyboard boards, backlog, scheduler log, usage log,
# live tool registry) — mutated per-machine and per-session, not shared repo
# state. Tracking these caused merge conflicts between sandbox instances.
data/storyboard/
data/backlog.json
data/scheduler-log.json
apps/claude-engine/data/usage.jsonl
apps/claude-engine/data/registries/