// Generated by Claude Engine · Agent Composer. Safe to edit by hand.
import { wordCount } from "../../lib/wordCount.js";

export const definition = {
  "id": "summarizer",
  "name": "Summarizer",
  "description": "Summarizes a task, reporting length first.",
  "model": "claude-opus-4-8",
  "systemPrompt": "You are a concise assistant. Summarize the user's text in one sentence.",
  "skills": [],
  "inputs": [{ "name": "task", "type": "text", "label": "Text to summarize" }]
};

/**
 * Run the agent. ctx = { provider, skills: { run(id, input) } }.
 */
export async function run(input, ctx) {
  const task = String(input.task || "");
  const length = wordCount(task);
  const prompt = [task, "Length: " + JSON.stringify(length)].filter(Boolean).join("\n\n");
  const reply = await ctx.provider.complete({
    system: definition.systemPrompt,
    prompt,
    model: definition.model,
  });
  return { reply: reply.text, model: reply.model, provider: reply.provider, skillOutputs: { length }, usage: reply.usage };
}
