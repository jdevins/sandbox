// Generated by Claude Engine · Skill Builder. Safe to edit by hand.
export const definition = {
  "id": "word-count",
  "name": "Word Count",
  "description": "Counts words and characters in some text.",
  "version": "1.0.0",
  "inputs": [
    { "name": "text", "type": "text", "label": "Text to process" }
  ]
};

/** Repeatable functionality as code. @param {object} input @param {{provider}} ctx */
export async function run(input, ctx) {
  const text = String(input.text || "");
  const words = text.split(/\s+/).filter(Boolean);
  return { words: words.length, chars: text.length };
}

export const tests = [
  { "name": "counts words", "input": { "text": "a b c" }, "expect": { "words": 3, "chars": 5 } },
  { "name": "empty", "input": { "text": "" }, "expect": { "words": 0, "chars": 0 } }
];
