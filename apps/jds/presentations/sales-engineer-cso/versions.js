// Single source of truth for which pitch drafts exist and their order, so the
// version toggle on every draft page never drifts out of sync. Add a new
// draft by pushing here, writing v{n}.js, and wiring its route in index.js.
export const VERSIONS = ['v1', 'v2', 'v3', 'v4', 'v5', 'v6', 'v7', 'v8', 'v9', 'v10', 'v11'];
